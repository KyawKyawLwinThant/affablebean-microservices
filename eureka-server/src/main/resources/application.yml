spring:
  application:
    name: eureka-server
server:
  port: 8083
eureka:
  client:
    register-with-eureka: false
    fetch-registry: false
    service-url:
      defaultZone: http://localhost:8083/eureka
